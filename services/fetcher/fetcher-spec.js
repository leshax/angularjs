describe('Fetcher service', function() {
    var fetcher;
    var data;

    beforeEach(module('myApp'));

    beforeEach(inject(function(_fetcher_) {
      fetcher = _fetcher_;
    }));

    beforeEach(function(){
      /* TODO */
    })

    it('querySearch() shoud return a promise with result', function() {
      /* TODO */
    });
    it('post() shoud return a promise after data was sent', function() {
      /* TODO */
    });
});
